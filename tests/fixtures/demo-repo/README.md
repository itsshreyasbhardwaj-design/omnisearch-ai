# demo-repo

A tiny, fake multi-language repository used to demo and test OmniSearch AI
without needing any external credentials. It is not a real application —
just enough surface area to exercise text search, regex search, filters,
and the code viewer across a few languages.
